/**
 * user.controller.ts Handles the fetching of level 1 salts & successful login requests
 */

import { UserObject, IUser } from '../../../model/User';
import { ImageObject, IImage } from '../../../model/Image';
import { Request, Response } from 'express';
import { CallbackError } from 'mongoose';

namespace ImageController {
  /**
   * 
   * @todo Insert Image controller function 
   * @todo Delete Image controller function
   * @todo more stuff
   */
}

export default ImageController;
